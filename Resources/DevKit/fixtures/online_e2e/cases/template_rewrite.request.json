{
  "messages": [
    {
      "content": "You are a chat template editor. Modify only what the user requests, keeping everything else unchanged. You must call save_chat_template exactly once with the full resulting template. Do not respond with plain text.",
      "role": "system"
    },
    {
      "content": "Modify the following chat template according to the user's request.\n\nIMPORTANT RULES:\n- Only change what the user specifically requests\n- If the user doesn't mention name or prompt, keep them unchanged\n- Use the user's preferred language for content\n\n[current template name]\nWeekly Status\n\n[current template prompt]\nTurn rough weekly notes into a polished status update with accomplishments, risks, and next steps.\n\n[user request]\nKeep the template name unchanged. Rewrite only the prompt so it becomes more concise and more executive.",
      "role": "user"
    }
  ],
  "model": "accounts/fireworks/models/deepseek-v4-pro",
  "stream": true,
  "temperature": 0.75,
  "tool_choice": {
    "function": {
      "name": "save_chat_template"
    },
    "type": "function"
  },
  "tools": [
    {
      "function": {
        "description": "Save the resulting chat template.",
        "name": "save_chat_template",
        "parameters": {
          "additionalProperties": false,
          "properties": {
            "emoji": {
              "description": "Single emoji representing the template purpose.",
              "type": "string"
            },
            "inherit_app_prompt": {
              "description": "Whether the template should inherit the application-wide system prompt.",
              "type": "boolean"
            },
            "name": {
              "description": "Short descriptive name for the template using concise language.",
              "type": "string"
            },
            "prompt": {
              "description": "System prompt that captures the conversation pattern and purpose.",
              "type": "string"
            }
          },
          "required": [
            "name",
            "emoji",
            "prompt",
            "inherit_app_prompt"
          ],
          "type": "object"
        },
        "strict": true
      },
      "type": "function"
    }
  ]
}
